<div class="carousel-item">
    <div class="title m-b-md">
    <img class="img-fluid" src="{{ asset('images/'.$img_title) }}" />
        <a class="link-title" href="{{route($route_title)}}">{{ $title }}</a>
    </div>
    <img class="img-fluid rounded" src="{{ asset('images/'.$url_image) }}" style="width: 600px; height: 300px; " />
    
    
</div>