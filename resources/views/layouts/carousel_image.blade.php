<div class="carousel-item">
    <img class="d-block w-100 rounded" src="{{ asset('images/'.$url_image) }}" >
    <div class="carousel-caption d-none d-md-block">
	    <h3>{{ $title }}</h3>
	    <p>{{ $paragraph }}</p>
  	</div>
    <!--img class="rounded" src="{{ asset('images/'.$url_image) }}" style="width: 600px; height: 300px; " /-->
    
 
</div>