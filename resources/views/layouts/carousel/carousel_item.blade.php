<div class="carousel-item {{ $active }}">
    <div class="title m-b-md">
    <img class="imgcarouselprim-{{ $route }} rounded" src="{{ asset('images/'.$url_image_prim) }}" />
        
    </div>
        
    <img class="imgcarouselsec-{{ $route }} rounded" src="{{ asset('images/'.$url_image_sec) }}" />
</div>

